1. I would fit my automated tests in a GitHub action that runs whenever code is pushed so that I could test the functionality of every change that I implement. That way, if something goes wrong in the code, I would know exactly where to fix it.
2. No. 
3. Navigation mode analyzes the page right after it reloads, so it would measure the full page-load experience such as load speed. On the other hand, snapshot mode analyzes the page in its current state, so it can check for accessibility and layout after interacting with the page.
4. Based on the Lighthouse results, we could minify JavaScript, reduce unused JavaScript, and minimize main-thread work to improve the CSE 110 shop site.

